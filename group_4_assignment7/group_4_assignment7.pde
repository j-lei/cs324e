Grid grid;
//Grid grid = new Grid(10, 10);


void setup(){
  size(500, 500);
  Grid grid = new Grid(10, 10);
}

void draw(){
  //Grid grid = new Grid(10, 10);
  //grid.spawn();
  //noLoop();

}

//void timer() {
//  if (millis() % 1000
